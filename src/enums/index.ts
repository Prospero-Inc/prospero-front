/* eslint-disable no-unused-vars */
export enum HttpMethod {
  GET = 'GET',
  POST = 'POST',
  PUT = 'PUT',
  PATCH = 'PATCH',
  DELETE = 'DELETE',
  OPTIONS = 'OPTIONS'
}

export enum CookiesEnum {
  NEXT_LOCALE = 'NEXT_LOCALE'
}
